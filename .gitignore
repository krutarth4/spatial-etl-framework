.idea/
.venv/
__pycache__/
logs/
tmp/
raw/
data/

# Secrets / local env — never commit real values
.env
.env.*
!.env.example

# Claude Code — commit the config folder and CLAUDE.md, but keep local settings private
.claude/settings.local.json
**/.claude/settings.local.json

# Benchmark results — generated at runtime
tests/bench/results/*.json
tests/bench/results/*.csv

# OS / editor noise
.DS_Store
._*
.ruff_cache/
*.swp
